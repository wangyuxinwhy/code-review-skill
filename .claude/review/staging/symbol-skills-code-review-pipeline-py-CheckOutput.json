{
  "stage": "symbol",
  "target": { "type": "symbol", "file": "skills/code-review/pipeline.py", "symbol": "CheckOutput", "lines": [243, 248] },
  "checks": [
    {
      "id": "type-annotations",
      "pass": true
    },
    { "id": "single-responsibility", "pass": true },
    { "id": "idiomatic-constructs", "pass": true },
    {
      "id": "naming-clarity",
      "pass": false,
      "note": "`cached` and `review` are asymmetric with `cached_symbols` and `review_symbols`. The bare names suggest scalar values or unrelated concepts; naming them `cached_files` and `review_files` would make the parallel with the `_symbols` fields explicit and self-documenting.",
      "annotations": [
        { "line": 244, "message": "`cached: list[str]` — rename to `cached_files` to match the parallel field `cached_symbols`" },
        { "line": 245, "message": "`review: list[str]` — rename to `review_files` to match the parallel field `review_symbols`" }
      ]
    }
  ]
}
