{
  "stage": "symbol",
  "target": { "type": "symbol", "file": "skills/code-review/pipeline.py", "symbol": "StagingEntry", "lines": [178, 185] },
  "checks": [
    {
      "id": "type-annotations",
      "pass": false,
      "note": "Three fields use `list[dict[str, Any]]` when more specific TypedDicts are already defined in the same file. `checks` should be `list[CheckResult]`; `targets` should be `list[dict[str, Any]]` narrowed to `list[TargetEntry]` or a dedicated TypedDict; `symbols` similarly. Using `Any`-keyed dicts here defeats the purpose of the surrounding type system.",
      "annotations": [
        { "line": 182, "message": "`list[dict[str, Any]]` — should be `list[CheckResult]`; `CheckResult` TypedDict is defined in the same file" },
        { "line": 184, "message": "`list[dict[str, Any]]` — should be `list[TargetEntry]` or a dedicated TypedDict if shape differs" },
        { "line": 185, "message": "`list[dict[str, Any]]` — should be `list[TargetEntry]` or a dedicated TypedDict if shape differs" }
      ]
    },
    { "id": "single-responsibility", "pass": true },
    {
      "id": "redundant-docstrings",
      "pass": true,
      "note": "Docstring 'A single staging file\u2019s content. Fields vary by stage type.' adds meaningful context not captured by the class name alone — explains the `total=False` choice."
    },
    { "id": "idiomatic-constructs", "pass": true },
    { "id": "naming-clarity", "pass": true }
  ]
}
