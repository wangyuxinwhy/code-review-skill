{
  "stage": "symbol",
  "target": { "type": "symbol", "file": "skills/code-review/pipeline.py", "symbol": "RefreshStats", "lines": [914, 922] },
  "checks": [
    { "id": "type-annotations", "pass": true },
    {
      "id": "single-responsibility",
      "pass": true
    },
    { "id": "idiomatic-constructs", "pass": true },
    {
      "id": "naming-clarity",
      "pass": false,
      "note": "`file_hits` and `symbol_hits` use plural forms, while the sibling `CacheStats` TypedDict uses singular forms (`file_hit`, `symbol_hit`, `file_miss`, `symbol_miss`). Within `RefreshStats` itself the names are internally consistent, but the divergence from `CacheStats`'s convention (singular counter names) creates a terminology split for the same concept across the same file.",
      "annotations": [
        { "line": 917, "message": "`file_hits` — uses plural; `CacheStats.file_hit` uses singular for the same counter concept" },
        { "line": 918, "message": "`symbol_hits` — uses plural; `CacheStats.symbol_hit` uses singular for the same counter concept" }
      ]
    }
  ]
}
